#include <stdio.h>
#include <string.h>

void exibevetor( char v[] )
{
	int i, tam;
	tam = strlen(v);
	for(i = 0; i < tam; i++ )
	{
		printf("%c", v[i]);
	}
	printf("\n");
}

void questao03(char str[], char cpf[], char nome[], char estado[], char uf[])
{
      int i, j, k; 	      
            
      for(i = 0; str[i] != '\0'; i++)
      {
      	while(str[i] != '\0')
      	
      }
	
}

